{
  "mcpServers": {
    "youtube-uploader": {
      "type": "stdio",
      "command": "${HOME}/.local/bin/youtube-uploader-mcp-darwin-arm64",
      "args": [
        "-client_secret_file",
        "${HOME}/.config/youtube-uploader/client_secret.json",
        "-working_dir",
        "${HOME}/.config/youtube-uploader"
      ],
      "env": {}
    }
  }
}
