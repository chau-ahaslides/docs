{
  "name": "ahaslides-kb-video-workspace",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "description": "Recording/encoding workspace for the KB tutorial-video pipeline (playwright + ffmpeg). Renders land in out-videos/.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "playwright": "^1.60.0"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  }
}
